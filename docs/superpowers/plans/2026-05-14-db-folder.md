# 数据库初始化文件夹 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 `db/` 文件夹，让新环境开发者能一键搭库，消除"换地方不知道怎么开始"的痛点。

**Architecture:** Prisma schema 仍为唯一真相来源，`db/init.sql` 只负责建库（CREATE DATABASE），建表由 `prisma db push` 完成。旧的根目录 `schema.sql` 移至 `db/legacy/java-schema.sql` 留档。`db/setup.md` 提供完整从零搭建手顺。

**Tech Stack:** MySQL, Prisma 6, Express 5

---

### Task 1: 创建 db/init.sql

**Files:**
- Create: `db/init.sql`

- [ ] **Step 1: 创建 db/init.sql**

```sql
-- PixelReel 数据库初始化
-- 仅建库，建表由 Prisma db push 完成（从 express-backend/prisma/schema.prisma 生成）
-- 用法: mysql -u root -p < db/init.sql

CREATE DATABASE IF NOT EXISTS pixelreel
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
USE pixelreel;
```

- [ ] **Step 2: 验证文件存在**

Run: `ls db/init.sql`
Expected: 文件存在且内容正确

- [ ] **Step 3: Commit**

```bash
git add db/init.sql
git commit -m "feat(db): add init.sql for database creation"
```

---

### Task 2: 移动 schema.sql 到 db/legacy/java-schema.sql

**Files:**
- Move: `schema.sql` → `db/legacy/java-schema.sql`
- Delete: `schema.sql`（根目录）

- [ ] **Step 1: 创建 db/legacy/ 目录并移动文件**

```bash
mkdir -p db/legacy
git mv schema.sql db/legacy/java-schema.sql
```

- [ ] **Step 2: 在文件头加注释标明 Java 后端用途**

在 `db/legacy/java-schema.sql` 最前面加：

```sql
-- ============================================================
-- Java Spring Boot 后端专用建表 SQL
-- Express + Prisma 后端不使用此文件，建表由 prisma db push 完成
-- 仅作为历史留档保留
-- ============================================================

```

- [ ] **Step 3: Commit**

```bash
git add db/legacy/java-schema.sql
git commit -m "refactor(db): move legacy Java schema to db/legacy/"
```

---

### Task 3: 创建 db/setup.md

**Files:**
- Create: `db/setup.md`

- [ ] **Step 1: 创建 db/setup.md**

```markdown
# PixelReel 开发环境搭建

从零开始搭建 PixelReel 开发环境。

## 前置要求

- Node.js 18+
- MySQL 8.0+
- npm

## 步骤

### 1. 安装 MySQL

如果还没有 MySQL，先安装并确保服务运行。

### 2. 创建数据库

```bash
mysql -u root -p < db/init.sql
```

这会创建 `pixelreel` 数据库（utf8mb4 字符集）。

### 3. 配置环境变量

```bash
cd express-backend
cp .env.example .env
```

编辑 `.env`，至少配置：

- `DATABASE_URL` — MySQL 连接串，格式：`mysql://用户名:密码@localhost:3306/pixelreel`
- `TMDB_API_KEY` — TMDB API 密钥（搜索电影/电视剧需要）

### 4. 安装依赖 & 建表

```bash
cd express-backend
npm install
npx prisma generate
npx prisma db push
```

`prisma db push` 会根据 `prisma/schema.prisma` 自动创建所有表。

### 5. 启动后端

```bash
cd express-backend
npm run dev
```

后端运行在 http://localhost:18889

### 6. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端运行在 http://localhost:18888，自动代理 `/api` 到后端。

## 数据说明

不提供 seed 数据，避免污染真实数据。通过前端搜索功能添加电影/电视剧/游戏记录即可。

## 常见问题

### prisma db push 报连接错误

检查 `.env` 中的 `DATABASE_URL` 格式和 MySQL 是否在运行。

### 端口被占用

后端端口可在 `.env` 的 `PORT` 修改，前端代理目标在 `frontend/vite.config.ts` 中修改。
```

- [ ] **Step 2: Commit**

```bash
git add db/setup.md
git commit -m "docs(db): add setup.md with step-by-step dev environment guide"
```

---

### Task 4: 更新 CLAUDE.md

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 在开发命令部分加入数据库初始化步骤**

将 CLAUDE.md 中"开发命令"部分更新，在现有代码块之前加入数据库初始化：

把：
```
## 开发命令

```bash
# Express 后端（默认）
cd express-backend && npm run dev        # tsx watch, 端口 18889
```

改为：
```
## 开发命令

```bash
# 数据库初始化（新环境首次）
mysql -u root -p < db/init.sql           # 建库，然后配置 .env
cd express-backend && npx prisma db push  # 建表（从 schema.prisma 生成）

# Express 后端（默认）
cd express-backend && npm run dev        # tsx watch, 端口 18889
```

- [ ] **Step 2: 更新项目结构部分，加入 db/ 说明**

在项目结构中 `src/` 之前加入：

```
db/                       ← 数据库初始化文件
  init.sql                ← 建库 SQL（仅 CREATE DATABASE）
  setup.md                ← 从零搭建手顺
  legacy/java-schema.sql  ← Java 后端建表 SQL（留档，Prisma 不使用）
```

同时将 `src/` 部分中 `pom.xml, schema.sql     ← 位于项目根目录` 改为 `pom.xml     ← 位于项目根目录`（因为 schema.sql 已移走）。

- [ ] **Step 3: 在深度文档部分加入 setup.md 引用**

在"深度文档"列表中加入：

```
- 开发环境搭建 → `db/setup.md`
```

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md with db/ folder and init steps"
```